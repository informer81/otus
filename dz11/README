Собираем контейнер
docker build -t nginx:test_nginx .

Запускем контейнер
docker run -d -p 80:80 nginx:test_nginx 

cerl http://localhost

попадаем на нашу кастомную страницу

в контейнер пробрасываем конфиг нжингса и стартовую страницу